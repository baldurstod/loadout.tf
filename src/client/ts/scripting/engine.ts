import { CanvasLayout, CanvasView, Graphics, Viewport } from 'harmony-3d';

export class Engine {
	Graphics = Graphics;
	CanvasLayout = CanvasLayout;
	CanvasView = CanvasView;
	Viewport = Viewport;
}
